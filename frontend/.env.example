# Leave VITE_API_BASE_URL unset locally — vite.config.ts already proxies
# /api to the backon backend it found at ../backend during scaffolding
# (see server.proxy), and requests are same-origin in most production
# deployments too. Only set this if the frontend and backend end up
# deployed to different origins.
VITE_API_BASE_URL=
