# Committed on purpose (unlike .env, which is gitignored): frontend and
# backend deploy as separate Vercel projects/domains, so production builds
# need this set. Routed through vercel.json's /api rewrite to the backend's
# custom domain rather than pointing straight at it, so the browser only
# ever talks to the frontend's own origin.
VITE_API_BASE_URL=/api
