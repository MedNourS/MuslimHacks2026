{
  "name": "care-circle",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:frontend": "bun run --filter frontend dev",
    "dev:backend": "bun run --filter backend dev",
    "build": "bun run --filter '*' build",
    "generate:client": "bun run --filter backend generate-client",
    "generate:docs": "bun run --filter backend generate-docs"
  }
}
